const articleTitle = document.querySelector("h4").textContent;
const articleFirstParagraph = document.querySelector("p").textContent;
const output = [{ title: articleTitle, firstParagraph: articleFirstParagraph }];
console.log(output);
